name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sqlfluff:
    name: SQL Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install sqlfluff sqlfluff-templater-dbt
          pip install dbt-databricks

      - name: Run SQLFluff
        run: |
          sqlfluff lint dbt_ov/models/ --dialect sparksql

  dbt_compile:
    name: dbt Compile
    runs-on: ubuntu-latest

    env:
      DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      DATABRICKS_HTTP_PATH: ${{ secrets.DATABRICKS_HTTP_PATH }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dbt
        run: pip install dbt-databricks

      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml << EOF
          dbt_ov:
            target: dev
            outputs:
              dev:
                type: databricks
                catalog: ov-pipeline
                schema: standardized
                host: ${{ secrets.DATABRICKS_HOST }}
                http_path: ${{ secrets.DATABRICKS_HTTP_PATH }}
                token: ${{ secrets.DATABRICKS_TOKEN }}
                threads: 1
          EOF

      - name: dbt deps
        working-directory: dbt_ov
        run: dbt deps

      - name: dbt compile
        working-directory: dbt_ov
        run: dbt compile